{
  "version": "v1",
  "hooks": [
    {
      "name": "Deploy Gate — Live Deploy Approval",
      "description": "Blocks live wrangler/npm deploys unless Raj confirms. Matcher scoped to execute_bash only. Shell script inspects the actual command — ignores staging, dev, rollback, list variants. Uses permissionDecision=ask to prompt Raj before proceeding to live.",
      "trigger": "PreToolUse",
      "matcher": "execute_bash",
      "action": {
        "type": "command",
        "command": "/Users/rajeevnaik/Code/xConvert.com/.kiro/hooks/deploy-gate-check.sh"
      }
    }
  ]
}
